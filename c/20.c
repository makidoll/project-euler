// Factorial digit sum

#include <stdio.h>

// long functionN(int max) {
// 	long sum = max;
// 	for (int i=max-1; i>0; --i) {
// 		printf("%lld\n", sum);
// 		sum *= i;
// 	} return sum;
// } 

void mulChar(char* a, int b) {
	printf("%s\n", a);
}

int main(int argc, char* argv[]) {
	
	char* digit = ;
	mulChar(digit, 100);
	return 0;
}